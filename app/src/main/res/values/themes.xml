<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.NotesManager" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primary_500</item>
        <item name="colorPrimaryVariant">@color/primary_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/primary_700</item>
        <item name="colorSecondaryVariant">@color/complimentary_700</item>
        <item name="colorOnSecondary">@color/white</item>
        <!--Remove Action Bar. -->
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
<!--        <item  name="android:textViewStyle">@style/TextViewStyle</item>-->
        <item name="fontFamily">@font/lato_regular</item>
    </style>

    <style name="TextViewStyle" parent="android:Widget.TextView">
        <item name="fontFamily">@font/lato_regular</item>
    </style>

    <style name="SplashScreenStyle" parent="Theme.NotesManager">
        <item name="android:windowFullscreen">true</item>
    </style>
</resources>